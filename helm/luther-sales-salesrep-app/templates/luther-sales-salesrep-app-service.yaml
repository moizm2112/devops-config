kind: Service
apiVersion: v1
metadata:
  name: {{ template "luther-sales-salesrep-app  .fullname" . }}
  labels:
    app: {{ template "luther-sales-salesrep-app  .name" . }}
    chart: "{{ .Chart.Name }}-{{ .Chart.Version }}"
    release: "{{ .Release.Name }}"
    namespace: "{{ .Release.Namespace }}"
spec:
  type: ClusterIP
  ports:
    - name: http-8080
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
  selector:
    app: {{ template "luther-sales-salesrep-app  .name" . }}
    release: "{{ .Release.Name }}"
