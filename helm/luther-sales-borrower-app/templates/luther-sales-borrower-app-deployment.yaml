apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ template "luther-sales-borrower-app .fullname" . }}
  labels:
    app: {{ template "luther-sales-borrower-app .name" . }}
    chart: "{{ .Chart.Name }}-{{ .Chart.Version }}"
    release: "{{ .Release.Name }}"
    heritage: "{{ .Release.Service }}"
    namespace: "{{ .Release.Namespace }}"
spec:
  replicas: 1
  strategy:
    type: RollingUpdate
  selector:
    matchLabels:
      app: {{ template "luther-sales-borrower-app .name" . }}
      release: {{ .Release.Name }}
  template:
    metadata:
      labels:
        app: {{ template "luther-sales-borrower-app .name" . }}
        chart: "{{ .Chart.Name }}-{{ .Chart.Version }}"
        release: "{{ .Release.Name }}"
        heritage: "{{ .Release.Service }}"
        namespace: "{{ .Release.Namespace }}"
    spec:
      containers:
        - name: luther-sales-borrower-app 
          image: "{{ .Values.imageRepository }}:{{ .Values.imageTag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          resources:
{{ toYaml .Values.resources | indent 12 }}
          ports:
            - name: http
              containerPort: 8080
          volumeMounts:
            - name: {{ template "luther-sales-borrower-app .name" . }}-configmap
              mountPath: /config/
          livenessProbe:
            httpGet:
              path: /{{ template "luther-sales-borrower-app .name" . }}/health
              port: http
            initialDelaySeconds: {{ .Values.livenessProbe.initialDelaySeconds }}
            periodSeconds: 20
            timeoutSeconds: 10
            failureThreshold: 10
          readinessProbe:
            httpGet:
              path: /{{ template "luther-sales-borrower-app .name" . }}/health
              port: http
            initialDelaySeconds: {{ .Values.readinessProbe.initialDelaySeconds }}
            periodSeconds: 20
            timeoutSeconds: 10
            failureThreshold: 10
      volumes:
        - name: {{ template "luther-sales-borrower-app .name" . }}-configmap
          configMap:
            name: {{ template "luther-sales-borrower-app .name" . }}
      {{- if .Values.imagePullSecrets }}
      imagePullSecrets:
{{ toYaml .Values.imagePullSecrets | indent 8 }}
      {{- end }}

