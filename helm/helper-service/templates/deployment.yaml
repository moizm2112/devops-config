apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ template "helper-service.fullname" . }}
  labels:
    app: {{ template "helper-service.name" . }}
    chart: "{{ .Chart.Name }}-{{ .Chart.Version }}"
    release: "{{ .Release.Name }}"
    heritage: "{{ .Release.Service }}"
    namespace: "{{ .Release.Namespace }}"
spec:
  replicas: 1
  strategy:
    type: RollingUpdate
  selector:
    matchLabels:
      app: {{ template "helper-service.name" . }}
      release: {{ .Release.Name }}
  template:
    metadata:
      labels:
        app: {{ template "helper-service.name" . }}
        chart: "{{ .Chart.Name }}-{{ .Chart.Version }}"
        release: "{{ .Release.Name }}"
        heritage: "{{ .Release.Service }}"
        namespace: "{{ .Release.Namespace }}"
    spec:
      containers:
        - name: helper-service
          image: "{{ .Values.imageRepository }}:{{ .Values.imageTag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          resources:
{{ toYaml .Values.resources | indent 12 }}
          ports:
            - name: http
              containerPort: 8080
          volumeMounts:
            - name: {{ template "helper-service.name" . }}-configmap
              mountPath: /config/
          envFrom:
            - configMapRef:
                name: {{ template "helper-service.name" . }} 
          # livenessProbe:
          #   httpGet:
          #     path: /{{ template "helper-service.name" . }}/health
          #     port: http
          #   initialDelaySeconds: {{ .Values.livenessProbe.initialDelaySeconds }}
          #   periodSeconds: 20
          #   timeoutSeconds: 10
          #   failureThreshold: 10
          # readinessProbe:
          #   httpGet:
          #     path: /{{ template "notification-service.name" . }}/health
          #     port: http
          #   initialDelaySeconds: {{ .Values.readinessProbe.initialDelaySeconds }}
          #   periodSeconds: 20
          #   timeoutSeconds: 10
          #   failureThreshold: 10
      volumes:
        - name: {{ template "helper-service.name" . }}-configmap
          configMap:
            name: {{ template "helper-service.name" . }}



