apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ template "user-management-service.name" . }}
  labels:
    app: {{ template "user-management-service .name" . }}
    chart: "{{ .Chart.Name }}-{{ .Chart.Version }}"
    release: "{{ .Release.Name }}"
    heritage: "{{ .Release.Service }}"
    namespace: "{{ .Release.Namespace }}"
spec:
  replicas: 1
  strategy:
    type: RollingUpdate
  selector:
    matchLabels:
      app: {{ template "user-management-service.name" . }}
      release: {{ .Release.Name }}
  template:
    metadata:
      labels:
        app: {{ template "user-management-service.name" . }}
        chart: "{{ .Chart.Name }}-{{ .Chart.Version }}"
        release: "{{ .Release.Name }}"
        heritage: "{{ .Release.Service }}"
        namespace: "{{ .Release.Namespace }}"
    spec:
      containers:
        - name: user-management-service
          image: "{{ .Values.imageRepository }}:{{ .Values.imageTag }}"
          imagePullPolicy: {{ .Values.imagepullPolicy }}
          resources:
{{ toYaml .Values.resources | indent 12 }}
          ports:
            - name: http
              containerPort: 8080
          volumeMounts:
            - name: {{ template "user-management-service.name" . }}-configmap
              mountPath: /config/
          envFrom:
            - configMapRef:
                name: {{ template "user-management-service.name" . }} 
          # livenessProbe:
          #   httpGet:
          #     path: /{{ template "user-management-service.name" . }}/health
          #     port: http
          #   initialDelaySeconds: {{ .Values.livenessProbe.initialDelaySeconds }}
          #   periodSeconds: 20
          #   timeoutSeconds: 10
          #   failureThreshold: 10
          # readinessProbe:
          #   httpGet:
          #     path: /{{ template "user-management-service.name" . }}/health
          #     port: http
          #   initialDelaySeconds: {{ .Values.readinessProbe.initialDelaySeconds }}
          #   periodSeconds: 20
          #   timeoutSeconds: 10
          #   failureThreshold: 10
      volumes:
        - name: {{ template "user-management-service .name" . }}-configmap
          configMap:
            name: {{ template "user-management-service .name" . }}



