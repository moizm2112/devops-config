apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ template "otp-service.name" . }}
  labels:
    app: {{ template "otp-service.name" . }}
    chart: "{{ .Chart.Name }}-{{ .Chart.Version }}"
    release: "{{ .Release.Name }}"
    heritage: "{{ .Release.Service }}"
    namespace: "{{ .Release.Namespace }}"
spec:
  replicas: 1
  strategy:
    type: RollingUpdate
  selector:
    matchLabels:
      app: {{ template "otp-service.name" . }}
      release: {{ .Release.Name }}
  template:
    metadata:
      labels:
        app: {{ template "otp-service.name" . }}
        chart: "{{ .Chart.Name }}-{{ .Chart.Version }}"
        release: "{{ .Release.Name }}"
        heritage: "{{ .Release.Service }}"
        namespace: "{{ .Release.Namespace }}"
    spec:
      containers:
        - name: otp-service
          image: "{{ .Values.imageRepository }}:{{ .Values.imageTag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          resources:
{{ toYaml .Values.resources | indent 12 }}
          ports:
            - name: http
              containerPort: 8080
          volumeMounts:
            - name: {{ template "otp-service.name" . }}-configmap
              mountPath: /config/
          envFrom:
            - configMapRef:
                name: {{ template "otp-service.name" . }} 
          # livenessProbe:
          #   httpGet:
          #     path: /{{ template "otp-service.name" . }}/health
          #     port: http
          #   initialDelaySeconds: {{ .Values.livenessProbe.initialDelaySeconds }}
          #   periodSeconds: 20
          #   timeoutSeconds: 10
          #   failureThreshold: 10
          # readinessProbe:
          #   httpGet:
          #     path: /{{ template "otp-service.name" . }}/health
          #     port: http
          #   initialDelaySeconds: {{ .Values.readinessProbe.initialDelaySeconds }}
          #   periodSeconds: 20
          #   timeoutSeconds: 10
          #   failureThreshold: 10
      volumes:
        - name: {{ template "otp-service.name" . }}-configmap
          configMap:
            name: {{ template "otp-service.name" . }}



